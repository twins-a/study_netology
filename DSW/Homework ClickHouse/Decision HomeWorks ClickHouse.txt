Решение домашнего задания ClickHouse.
----------------------------------------

Скачал yml. Установил контейнер в докер.

Запустил:
	docker run -d --name clickhouse-server -p 9000:9000 --ulimit nofile=262144:262144 yandex/clickhouse-server

Посмотрел контейнеры (и их id)
	docker ps

Посмотреть файлы
	ls
	



Создал базу homework:
	CREATE DATABASE homework


Создал таблицу metrika:
	CREATE TABLE homework.metrika ("EventDate" Date, "CounterID" UInt32, "UserID" UInt64, "RegionID" UInt32) ENGINE = MergeTree() PARTITION BY toYYYYMM(EventDate) ORDER BY (CounterID, EventDate, intHash32(UserID))


Копирую файл в docker
	docker cp metrika_sample.tsv 3fac841431ce:/

Залил данные в таблицу
	cat metrika_sample.tsv | clickhouse-client --database homework --query "INSERT INTO metrika FORMAT TSV"


Запросы тестовые:
select * from homework.metrika limit 10
select count() from homework.metrika


Запрос по ДЗ:

	а) select UserID, count() from homework.metrika group by UserID order by count() desc limit 1
или
	б) select UserID from homework.metrika group by UserID order by count() desc limit 1
	
	Ответ: Пользователь 1313448155240738815 просмотрел больше всего страниц